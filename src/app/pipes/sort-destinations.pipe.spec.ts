import { FilterPipeFrom } from './sort-destinations .pipe';

describe('SortFlightDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeFrom();
    expect(pipe).toBeTruthy();
  });
});
