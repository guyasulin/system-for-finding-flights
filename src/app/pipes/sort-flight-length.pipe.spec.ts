import { SortFlightLengthPipe } from './sort-flight-length.pipe';

describe('SortFlightLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFlightLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
